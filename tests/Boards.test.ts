import { movePiece } from '../Board';
import { BoardState, Board, Move } from '../types/Board';

xdescribe('dud', () => {
  test('should be a dud', () => {
    console.warn('Not implemented');
    expect(true).toBe(true);
  });
})